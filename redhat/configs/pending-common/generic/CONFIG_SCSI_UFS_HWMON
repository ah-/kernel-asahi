# Symbol: SCSI_UFS_HWMON [=n]
# Type  : bool
# Defined at drivers/scsi/ufs/Kconfig:203
#   Prompt: UFS  Temperature Notification
#   Depends on: SCSI_LOWLEVEL [=y] && SCSI [=y] && (SCSI_UFSHCD [=n]=HWMON [=y] || HWMON [=y]=y)
#   Location:
#     -> Device Drivers
#       -> SCSI device support
#         -> SCSI low-level drivers (SCSI_LOWLEVEL [=y])
# 
# 
# 
# CONFIG_SCSI_UFS_HWMON is not set
