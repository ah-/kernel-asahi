# Symbol: DRM_VIRTIO_GPU_KMS [=y]
# Type  : bool
# Defined at drivers/gpu/drm/virtio/Kconfig:15
#   Prompt: Virtio GPU driver modesetting support
#   Depends on: HAS_IOMEM [=y] && DRM_VIRTIO_GPU [=m]
#   Location:
#     -> Device Drivers
#       -> Graphics support
#         -> Virtio GPU driver (DRM_VIRTIO_GPU [=m])
#           -> Virtio GPU driver modesetting support (DRM_VIRTIO_GPU_KMS [=y])
# 
# 
# 
CONFIG_DRM_VIRTIO_GPU_KMS=y
